# apps/backend/dockerfile
FROM node:22-bookworm-slim

# Install pnpm globally
RUN npm i -g pnpm@9.15.0

# Work from backend app directory (code is bind-mounted at runtime)
WORKDIR /app/apps/backend

EXPOSE 3000
CMD ["pnpm", "run", "dev"]
