import { StyleSheet } from 'react-native';

export const profileSettingsStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    paddingTop: 65,
  },
  backButton: {
    marginBottom: 10,
  },
  backText: {
    fontSize: 18,
  },
  title: {
    fontSize: 22,
    fontWeight: '700',
    marginTop: 80,
    marginBottom: 40,
    textAlign: 'center',
  },
  input: {
    padding: 17,
    borderRadius: 18,
    fontSize: 16,
    marginBottom: 12,
  },
  button: {
    padding: 16,
    borderRadius: 18,
  },
  buttonText: {
    fontSize: 16,
    textAlign: 'center',
    fontWeight: 'bold',
  },
});
